[package]
name = "restrav_validator"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.20", features = ["extension-module"] }
image = { version = "0.24", optional = true }

[features]
visual-validation = ["image"]
