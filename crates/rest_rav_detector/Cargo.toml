[package]
name = "rest_rav_detector"
version = "0.1.0"
edition = "2021"

[dependencies]
onnxruntime = "1.16"
image = "0.24"
serde = { version = "1.0", features = ["derive"] }

[features]
visual-validation = ["onnxruntime", "image"]
