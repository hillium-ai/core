[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "hillium_vr"
version = "0.1.0"
authors = [
    {name = "Hillium Team"}
]

[tool.maturin]
bindings = "pyo3"

[lib]
name = "hillium_vr"
crate = "hillium_vr"
